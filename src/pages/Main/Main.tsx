import * as React from "react";

interface MainProps {}

const Main: React.FunctionComponent<MainProps> = props => {
  return (
    <div>
      <div>Main</div>
    </div>
  );
};

export default Main;
